# TO-do-app
A full-stack Todo App built with React (frontend) and Node.js + Express + MongoDB (backend). Features: User signup/login with JWT authentication, protected routes, add/update/delete todos, and task tracking with UI animations.
